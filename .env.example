# Telegram Bot Token (Required)
# Get this from BotFather on Telegram
TELEGRAM_BOT_TOKEN=""

# Telegram Bot Token for Staging/Development (Optional)
# Used if you run the bot with the --staging flag
TELEGRAM_BOT_TOKEN_STAGING=""

# Your personal Telegram User ID (Required)
# The bot will send error notifications here. Get your ID from bots like @userinfobot
ADMIN_TELEGRAM_ID=""

# Comma-separated list of Telegram Group IDs where the bot should work (Optional)
# Leave empty to allow in any group (if not restricted by user IDs). Get group IDs using the bot in the group (it might log unauthorized access attempts to the admin). Negative sign is important.
AUTHORIZED_GROUP_IDS=""

# Comma-separated list of Telegram User IDs authorized to use the bot (Optional)
# The ADMIN_TELEGRAM_ID is automatically authorized. Leave empty to allow all users (if not restricted by group IDs).
AUTHORIZED_USER_IDS=""

# Base URL for the Google Gemini API (Optional)
# Defaults to https://generativelanguage.googleapis.com if not set
GEMINI_API_BASE_URL="https://generativelanguage.googleapis.com"

# Specific Gemini model to use for image generation (Optional)
# Defaults to gemini-2.0-flash-exp-image-generation if not set (see config.py)
GEMINI_IMAGE_MODEL="gemini-2.0-flash-exp-image-generation"

# Specific Gemini model to use for text generation (Optional)
# Defaults to gemini-2.5-flash-preview-04-17 if not set (see config.py)
GEMINI_TEXT_MODEL="gemini-2.5-flash-preview-04-17"

# Google Gemini API Key(s) (Required)
# Obtain from Google AI Studio or Google Cloud Console.
# You can provide multiple keys separated by commas, the bot will cycle through them.
GEMINI_API_KEYS="YOUR_API_KEY_1,YOUR_API_KEY_2,..."

DEFAULT_DISPLAY_LLM_TEXT="True"